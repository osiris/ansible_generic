---

- name: "Restarting {{ generic_service_name }} services"
  service:
    name: "{{ service }}"
    state: restarted
  with_items: "{{ generic_service_name }}"
  loop_control:
    loop_var: service
    label: "{{ service }}"
